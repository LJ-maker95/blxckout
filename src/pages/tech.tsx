import type { NextPage } from "next";
import Head from "next/head";
import { TechView } from "../views";

const Tech: NextPage = (props) => {
  return (
    <div>
      <Head>
        <title>BLXCKOUT NFT</title>
        <meta/>
      </Head>
      <TechView />
    </div>
  );
};

export default Tech;
